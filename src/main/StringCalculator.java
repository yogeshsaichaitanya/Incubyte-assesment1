package main;

public class StringCalculator {
    public int add(String numbers) {
        if (numbers.isEmpty())
            return 0;

        int n=numbers.length();
        int sum=0;



        return -1;
    }
}
